server:
  port: 8084

spring:
  application:
    name: leadbridge-report-service
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${KEYCLOAK_ISSUER_URI:http://localhost:8180/realms/leadbridge}

management:
  endpoints:
    web:
      exposure:
        include: health
